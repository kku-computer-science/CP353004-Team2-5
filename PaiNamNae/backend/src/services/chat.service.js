// src/services/chat.service.js

const getMessages = async (chatRoomId) => {
  return [];
};

const sendMessage = async (data) => {
  return data;
};

module.exports = {
  getMessages,
  sendMessage
};
